# Remove the slide-list refresh button

- Issue: #414, follow-up to #412 / #413.
- Branch: `codex/issue-414`; base: `f1774b3`.
- Outcome: no “一覧を更新” button; keep row list, counts, creation, page controls
  and retry for failed reads. No API, DB, auth, projection or sidebar changes.

## Plan

1. [x] Inspect parent Issue, current list/styles/tests and prior contract.
2. [x] Remove refresh button and unused toolbar; adapt existing tests and contract.
3. [x] Run checks and existing E2E, inspect screenshot and review the diff.
4. [x] Prepare verification handoff; exact required CI/merge status is in the PR.

## Verification

- `pnpm check`: passed format/lint/typecheck, 438 unit / 92 component tests,
  configuration checks and production build.
- `pnpm test:e2e`: all 32 Chromium tests passed, retries=0, including no refresh
  button, 390px/1280px geometry/axe, pagination and existing CRUD/projection.
- Visually inspected synthetic slide-list-390.png; button and empty toolbar space
  are gone. Wide screenshot is generated by the same existing E2E.
- Separate diff review and `git diff --check`: passed. Only refresh UI/wrapper and
  its obsolete style are removed; request, cursor, retry and cleanup code unchanged.
- No new tests for this simple removal; existing tests retain loading, empty,
  error focus/retry, pagination, escaped text and unmount cleanup coverage.
- No secrets, real data, API/DB/schema/auth changes or dependencies added.
